package com.aiw.${module}.mapper;

import com.aiw.mapper.BaseMapper;
import com.aiw.${module}.entity.${component};

/** 
 * 说明：${module}.${componentLower}
 * 创建人：aiw
 * 创建时间：${nowDate?string("yyyy-MM-dd")}
 */

public interface ${component}Mapper extends BaseMapper<${component}> {

	

}
